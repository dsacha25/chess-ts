import styled from 'styled-components';
import ImageContainer from '../../../containers/image-container/image-container.component';

export const ProfilePicture = styled(ImageContainer)`
	border-radius: 15px;
`;
